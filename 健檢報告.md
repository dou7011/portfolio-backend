# Portfolio Backend 健檢報告

更新日期：2026-07-11

## 一、執行摘要

本次針對專案程式碼、路由授權邏輯、資料庫 schema 與部署設定進行靜態健檢，重點涵蓋：
- 資安（Authentication/Authorization、輸入驗證、錯誤處理、資料外洩風險）
- 效能（查詢效率、序列化成本、可擴充性）
- 維運（可觀測性、設定管理、文件一致性）

整體評估結果：
- 架構方向正確（JWT + RBAC + 模組化路由）
- 尚有 3 個中風險與 2 個低風險待處理，建議依優先順序逐步修復

## 二、風險分級結果

- 高風險：0 項（已全數修復）
- 中風險：3 項
- 低風險：2 項

---

## 三、已修復項目

| # | 嚴重度 | 項目 | 修復說明 |
|---|--------|------|---------|
| 1 | 高風險 | 履歷更新 API 實際未執行更新 | `updateResumeController` 現已在確認履歷存在後呼叫 `updateResume()`，資料可正確寫入 DB |
| 2 | 中風險 | deleteUserController 回傳 `error.message` 洩露內部資訊 | 500 錯誤現在統一回傳 `'移除失敗'`，不再暴露原始例外訊息 |
| 3 | 中風險 | CORS 白名單硬編碼在程式中 | 已改為從 `wrangler.jsonc` 的 `ALLOWED_ORIGINS` 讀取白名單，程式可依環境調整 |
| 4 | 中風險 | JWT Payload 欄位過少，缺乏標準 claims | 已補上 `iss`、`aud`、`iat`、`nbf`、`jti` 等標準 claims，token 結構更完整 |
| 5 | 低風險 | README 與實際專案結構不同步 | 已更新目錄樹（`routes/` → `modules/`）、API 路由表（加入 users 路由、修正 resume 路徑）、資料表說明（補齊 6 張表）及注意事項，與現行程式碼完全一致 |

---

## 四、待修復問題（依嚴重度）

## 中風險

### 1. 未對登入 API 進行速率限制（易受暴力嘗試）

- **位置：** [src/modules/auth/auth.route.ts](src/modules/auth/auth.route.ts)
- **現況描述：**  
  `POST /api/auth/login` 目前沒有任何速率限制、IP 封鎖、帳號鎖定或遞增延遲機制。攻擊者可以無限次嘗試不同密碼組合，暴力破解或進行憑證填充（Credential Stuffing）攻擊的成本極低。
- **潛在影響：**
  - 帳號密碼遭暴力猜測
  - 憑證填充攻擊（使用外洩資料庫的帳密清單）
  - 造成 D1 資料庫不必要的查詢負載
- **建議解決方案：**
  1. 利用 Cloudflare Workers KV 或 Durable Objects 實作計數器：同一 IP 於 15 分鐘內登入失敗超過 5 次，封鎖 15 分鐘
  2. 或直接在 Cloudflare Dashboard 的 WAF 規則設定速率限制（零程式碼方案）
  3. 登入失敗回應加入固定延遲（如 1 秒），降低自動化攻擊效率

---

### 2. JSON.parse 缺乏保護，資料異常時會直接拋 500

- **位置：**
  - [src/modules/resume/resume.service.ts](src/modules/resume/resume.service.ts)（`getResumeByLang` 中的 `skills`、`experience`、`education`、`certifications` 欄位）
  - [src/modules/users/users.service.ts](src/modules/users/users.service.ts)（`getAllUsersService` / `getUserByIdService` 中的 `roles_json` 欄位）
- **現況描述：**  
  從 DB 讀回的 JSON 字串欄位直接呼叫 `JSON.parse()` 而無 try/catch 保護。若資料庫中有一筆欄位值為 `null`、空字串或格式錯誤的 JSON（例如手動寫入時的筆誤），`JSON.parse` 會直接拋出例外，導致整支 API 回傳 500，影響所有使用者的正常讀取。
- **潛在影響：**
  - 單筆髒資料造成整個讀取 API 不可用
  - 難以區分是「資料問題」還是「程式問題」
- **建議解決方案：**

  在 `src/utils/` 建立通用工具函式：
  ```typescript
  // src/utils/safeJsonParse.ts
  export function safeJsonParse<T>(value: unknown, fallback: T): T {
    if (typeof value !== 'string' || value.trim() === '') return fallback;
    try {
      return JSON.parse(value) as T;
    } catch {
      console.warn('[safeJsonParse] 無法解析 JSON，回傳預設值。原始值：', value);
      return fallback;
    }
  }
  ```
  
  接著在 service 層替換所有裸 `JSON.parse`：
  ```typescript
  // resume.service.ts
  skills: safeJsonParse(resume.skills, []),
  experience: safeJsonParse(resume.experience, []),
  education: safeJsonParse(resume.education, []),
  certifications: safeJsonParse(resume.certifications, []),

  // users.service.ts
  roles: safeJsonParse(user.roles_json, []),
  ```

---

### 3. authGuard 每次請求都執行 5 表 JOIN，DB 壓力偏高

- **位置：** [src/middleware/authGuard.ts](src/middleware/authGuard.ts)（第 40 行起的查詢）
- **現況描述：**  
  每個需要授權的 API 請求都會執行 `users → user_roles → roles → role_permissions → permissions` 的五表 JOIN 查詢，即時取得使用者的角色與權限清單。在流量低時無感，但隨著請求並發量提升，此查詢將成為 D1 的主要壓力點，且每次請求的延遲都會包含這次 DB round-trip。
- **潛在影響：**
  - 並發升高時 DB 查詢排隊延遲
  - 每個 API 的回應時間都受限於這次 JOIN 的速度
- **建議解決方案（擇一或組合）：**

  **方案 A（推薦）— 在 JWT Payload 內嵌入權限清單：**  
  登入時一次查詢權限並寫入 token，authGuard 只需驗證 token 簽章，不用查 DB：
  ```typescript
  // auth.service.ts 登入時
  const { results: permRows } = await db.prepare(`
    SELECT p.action FROM permissions p
    JOIN role_permissions rp ON p.id = rp.permission_id
    JOIN user_roles ur ON rp.role_id = ur.role_id
    WHERE ur.user_id = ?
  `).bind(user.id).all();
  
  const payload = {
    id: user.id,
    email: user.email,
    permissions: permRows.map(r => r.action),
    tokenVersion: user.token_version, // 用於強制登出
    exp,
  };
  ```
  
  **方案 B — 短 TTL 快取（Cloudflare KV）：**  
  以 `user:{id}:permissions` 為 key，快取 5 分鐘，authGuard 優先讀快取，miss 時才查 DB。

---

## 低風險

### 4. resumes.lang 未宣告 UNIQUE，依賴程式碼保護資料唯一性

- **位置：** [schema.sql](schema.sql)（resumes 表定義）
- **現況描述：**  
  `resumes` 表的 `lang` 欄位只宣告 `NOT NULL`，沒有 `UNIQUE` 約束。目前 `updateResume` service 的邏輯是「先查再 INSERT 或 UPDATE」，在正常情況下不會產生重複。但若發生並發請求或直接對 DB 操作，可能插入兩筆相同 `lang` 的資料，導致 `getResumeByLang` 的 `LIMIT 1` 查詢每次可能取得不同結果，造成資料不一致。
- **潛在影響：**  
  同語言出現多筆履歷，前端讀取結果不穩定，且難以察覺。
- **建議解決方案：**  
  在 `schema.sql` 補上 UNIQUE 約束，並在 service 改用 UPSERT 語法：
  ```sql
  -- schema.sql：調整 lang 欄位
  lang TEXT NOT NULL UNIQUE,
  
  -- 或另外建立 unique index（不修改 table 定義時）
  CREATE UNIQUE INDEX IF NOT EXISTS idx_resumes_lang ON resumes(lang);
  ```

---

### 5. role_permissions 缺少外鍵約束，資料完整性無資料庫層保護

- **位置：** [schema.sql](schema.sql)（role_permissions 表定義）
- **現況描述：**  
  `role_permissions` 表目前只有 composite PK，沒有宣告 `role_id → roles(id)` 和 `permission_id → permissions(id)` 的外鍵（FOREIGN KEY）。若手動刪除一個 role 或 permission，對應的 `role_permissions` 記錄不會自動清除，形成孤兒資料（orphan records），可能導致 authGuard 的 JOIN 查詢回傳錯誤的空白結果。
- **潛在影響：**  
  - 資料庫層無法保障參照完整性，需完全依賴應用程式邏輯
  - 孤兒記錄可能造成 authGuard 授權查詢結果異常
- **建議解決方案：**  
  補上外鍵並加入 CASCADE 策略：
  ```sql
  CREATE TABLE IF NOT EXISTS role_permissions (
    role_id INTEGER NOT NULL,
    permission_id INTEGER NOT NULL,
    PRIMARY KEY (role_id, permission_id),
    FOREIGN KEY (role_id) REFERENCES roles(id) ON DELETE CASCADE,
    FOREIGN KEY (permission_id) REFERENCES permissions(id) ON DELETE CASCADE
  );
  ```

---

### 6. 日誌策略不一致，事故調查困難

- **位置：** [src/modules/users/users.controller.ts](src/modules/users/users.controller.ts)（第 64 行），以及整個 `src/` 的錯誤處理
- **現況描述：**  
  目前只有 `createUserController` 在 500 時呼叫 `console.error`，其他 controller 的 catch 區塊完全沒有任何日誌輸出。當 API 回傳 500 時，在 Cloudflare Workers 的 Logs 中幾乎無法找到對應的錯誤訊息，也無法關聯是哪個請求觸發的。
- **潛在影響：**  
  - 生產環境發生問題時，難以快速定位根因
  - 無法區分是偶發性錯誤還是系統性問題
- **建議解決方案：**  
  建立統一的日誌工具，至少確保每個 catch 都有輸出：
  ```typescript
  // src/utils/logger.ts
  export const logger = {
    error: (context: string, error: unknown) => {
      console.error(JSON.stringify({
        level: 'error',
        context,
        message: error instanceof Error ? error.message : String(error),
        timestamp: new Date().toISOString(),
      }));
    }
  };
  ```
  
  在每個 controller 的 catch 區塊統一呼叫：
  ```typescript
  } catch (error) {
    logger.error('deleteUserController', error);
    return c.json({ success: false, message: '移除失敗' }, 500);
  }
  ```

---

## 五、效能觀察與建議

### 現況
- 查詢採參數綁定（避免 SQL Injection），這點良好
- 使用 D1 與 Worker，適合輕量 API
- JSON 欄位儲存使 schema 彈性高，但查詢/驗證成本偏應用層

### 優化優先順序
1. 降低 authGuard 權限查詢頻率（見第 3 項：JWT Payload 內嵌權限或 KV 快取）
2. 補齊 `resumes.lang` UNIQUE index 與 `role_permissions` FK 約束
3. `safeJsonParse` 集中處理所有 JSON 欄位，避免分散的裸 `JSON.parse`

---

## 六、資安強化清單（建議兩週內完成）

1. 導入登入 rate limit 與失敗節流（最高優先）
2. 實作 `safeJsonParse` 防止 JSON 解析炸掉整支 API
3. 補齊 DB 約束（UNIQUE/FK）
4. 統一日誌策略

---

## 七、建議測試補強

- Auth
  - 登入成功／失敗／停用帳號
  - 無效 token、過期 token、權限不足
  - 連續失敗登入觸發速率限制
- Resume
  - PUT /api/resume 實際寫入驗證（含 `updated_at` 變更）
  - `lang` 欄位非法值（非 `en`/`zh`）
  - DB 中 JSON 欄位為 `null` 或格式錯誤時的降級行為
- Users
  - 建立重複 email、角色覆寫、刪除 cascade 行為

---

## 八、結論

高風險缺陷（履歷更新未生效）與錯誤訊息洩露問題均已修復，另外 CORS 與 JWT 標準 claims 也已補齊。當前主要待處理項目為：登入防爆破（中風險最高優先）、JSON 解析保護、authGuard 權限查詢頻率，以及資料庫約束補齊。完成上述項目後，整體資安與效能可達到可長期維運的水準。
